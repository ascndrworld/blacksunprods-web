---
import { SITE } from "../config/site.mjs";
// Header global compartido — animación estilo Somos
// Top: transparente, 100px, logo 44px
// Scroll (>20px): 70px, fondo rgba(0,0,0,0.88) + blur(10px), logo 28px
// Transición 0.4s ease
---

<header class="site-header" id="siteHeader">
  <a href="/" class="site-logo" aria-label={`${SITE.name} — inicio`}>
    <img src={SITE.logo} alt={SITE.name} width="964" height="176"/>
  </a>
  <button class="site-hamburger" id="siteHamburger" aria-label="Abrir menú" aria-controls="siteMenu" aria-expanded="false">
    <span></span><span></span><span></span>
  </button>
</header>

<nav class="site-menu" id="siteMenu" aria-hidden="true">
  <button class="site-menu-close" id="siteMenuClose" aria-label="Cerrar menú">✕</button>
  <ul class="site-menu-links">
    <li><a href="/">Home</a></li>
    <li><a href="/proyectos">Proyectos</a></li>
    <li><a href="/ayuntamientos">Ayuntamientos</a></li>
    <li><a href="/sonido-e-iluminacion">Sonido e iluminación</a></li>
    <li><a href="/contacto">Contacto</a></li>
  </ul>
  <div class="site-menu-bottom">
    <div class="site-menu-socials">
      <a href={SITE.instagram} target="_blank" rel="noopener">Instagram</a>
      <span class="site-menu-dot">·</span>
      <a href="/contacto">Escríbenos</a>
    </div>
  </div>
</nav>

<script is:inline>
  (function() {
    var header = document.getElementById('siteHeader');
    var menu = document.getElementById('siteMenu');
    var btn = document.getElementById('siteHamburger');
    var closeBtn = document.getElementById('siteMenuClose');

    function setMenu(open) {
      if (!menu || !btn) return;
      menu.classList.toggle('open', open);
      btn.classList.toggle('open', open);
      btn.setAttribute('aria-expanded', open ? 'true' : 'false');
      menu.setAttribute('aria-hidden', open ? 'false' : 'true');
      document.body.style.overflow = open ? 'hidden' : '';
    }
    function toggleMenu() { setMenu(!menu.classList.contains('open')); }

    if (btn) btn.addEventListener('click', toggleMenu);
    if (closeBtn) closeBtn.addEventListener('click', function() { setMenu(false); });
    document.addEventListener('keydown', function(e) {
      if (e.key === 'Escape' && menu && menu.classList.contains('open')) setMenu(false);
    });

    function getScrollY() {
      return window.scrollY
        || window.pageYOffset
        || document.documentElement.scrollTop
        || document.body.scrollTop
        || 0;
    }
    function isMobile() {
      return window.matchMedia('(max-width: 768px)').matches;
    }
    // El header se vuelve negro/compacto al SALIR del hero (#home), no a los 20px.
    // Umbral = alto del hero menos un margen, para que la transición acompañe la
    // llegada de la primera sección. Constante (no usa el alto del header) para
    // que el cambio de altura 100→70 no provoque oscilación en el límite.
    var hero = document.getElementById('home');
    function scrollThreshold() {
      return hero ? Math.max(20, hero.offsetHeight - 90) : 20;
    }
    function onScroll() {
      if (!header) return;
      var y = getScrollY();
      header.classList.toggle('scrolled', y > scrollThreshold());
      // Móvil: header transparente y estático, visible solo arriba del todo
      if (isMobile()) {
        header.classList.toggle('header-hidden', y > 8);
      } else {
        header.classList.remove('header-hidden');
      }
    }
    // El contenedor real puede ser window, document o body según el layout
    window.addEventListener('scroll', onScroll, { passive: true });
    document.addEventListener('scroll', onScroll, { passive: true });
    document.body.addEventListener('scroll', onScroll, { passive: true });
    onScroll();
  })();
</script>

<style is:global>
  /* ── HEADER GLOBAL — estilo Somos ──
     Transparente total arriba. Transición de blur(0→10) y bg
     evita el "corte" visual que dejaba el ::before. */
  .site-header {
    position: fixed;
    top: env(safe-area-inset-top, 0px); left: 0; right: 0;
    z-index: 9999;
    display: flex;
    align-items: center;
    justify-content: center;
    height: 100px;
    width: 100%;
    padding: 0 60px;
    background-color: transparent;
    backdrop-filter: none;
    -webkit-backdrop-filter: none;
    transition: height 0.4s ease, background-color 0.4s ease, backdrop-filter 0.4s ease, -webkit-backdrop-filter 0.4s ease, transform 0.35s ease;
  }
  /* Sobre el hero: grande y transparente. Al pasar a la primera sección (lo activa
     el JS al salir del hero) transiciona limpio a barra negra y se encoge. */
  /* Opaco del todo, no 0.92: con un 8% de transparencia el texto que pasa por
     debajo se translucía y las líneas quedaban cortadas por la mitad contra el
     borde inferior del header. Sobre fondo negro el blur no aportaba nada
     (no hay color que difuminar), así que solo se veía la fuga. */
  .site-header.scrolled {
    height: 70px;
    background-color: #000;
  }

  .site-logo {
    display: flex;
    align-items: center;
    /* El wordmark pesa abajo (la "A" alta deja hueco arriba): el centro de masa
       cae ~5px por debajo del centro geométrico. Lo subimos para que quede
       ópticamente centrado y a la misma altura que el hamburger. */
    transform: translateY(-5px);
  }
  .site-logo img {
    height: 44px;
    width: auto;
    display: block;
    transition: height 0.4s ease;
    /* sombra sutil para que el logo no se pierda sobre el hero */
    filter: drop-shadow(0 1px 5px rgba(0, 0, 0, 0.5));
  }
  .site-header.scrolled .site-logo img {
    height: 28px;
  }

  .site-hamburger {
    position: absolute;
    top: 50%;
    right: clamp(24px, 5vw, 88px);
    transform: translateY(-50%);
    background: none;
    border: none;
    cursor: pointer;
    padding: 4px;
    display: flex;
    flex-direction: column;
    gap: 5px;
    z-index: 20;
    width: 30px;
    height: 30px;
    justify-content: center;
    align-items: flex-end;
    /* sombra sutil para no perderse sobre el contenido al deslizar */
    filter: drop-shadow(0 1px 3px rgba(0, 0, 0, 0.55));
  }
  .site-hamburger span {
    display: block;
    width: 22px;
    height: 2px;
    background: #F3F3F0;
    transition: all 0.3s cubic-bezier(0.77,0,0.175,1);
    transform-origin: center;
  }
  .site-hamburger.open span:nth-child(1) { transform: translateY(7px) rotate(45deg); }
  .site-hamburger.open span:nth-child(2) { opacity: 0; transform: scaleX(0); }
  .site-hamburger.open span:nth-child(3) { transform: translateY(-7px) rotate(-45deg); }
  /* La flecha del hamburger se anima desde el centro: el botón ya está centrado vertical */

  /* ── MENÚ OVERLAY ── */
  .site-menu {
    position: fixed;
    inset: 0;
    z-index: 99999;
    display: flex;
    flex-direction: column;
    align-items: center;
    justify-content: center;
    /* Opaco: con 0.9 se transparentaba la página por debajo y los enlaces del
       menú se leían encima del contenido. */
    background: #000;
    opacity: 0;
    pointer-events: none;
    transition: opacity 0.35s ease;
  }
  .site-menu.open { opacity: 1; pointer-events: all; }

  /* La ✕ se coloca justo encima del hamburger: mismo inset a la derecha y
     centrada en la vertical del header (100px → 50px). */
  .site-menu-close {
    position: absolute;
    top: 50px;
    right: clamp(24px, 5vw, 88px);
    transform: translateY(-50%);
    background: none;
    border: none;
    color: rgba(255,255,255,0.7);
    font-size: 18px;
    cursor: pointer;
    padding: 8px;
    line-height: 1;
    transition: color 0.2s, transform 0.3s ease;
  }
  .site-menu-close:hover { color: #F3F3F0; transform: translateY(-50%) rotate(90deg); }

  .site-menu-links {
    list-style: none;
    padding: 0 20px;
    margin: 0;
    text-align: center;
    max-width: 100%;
  }
  .site-menu-links li { line-height: 1; }
  .site-menu-links li a {
    font-family: "BSP Helvetica", sans-serif;
    /* Fluido, no 60px fijos: el enlace más largo ("sonido e iluminación") se
       salía de pantalla en móvil. El clamp sigue la escala que tenían los
       breakpoints (60 en escritorio, ~44 en tablet, ~30 en móvil pequeño). */
    font-size: clamp(28px, 4.2vw, 60px);
    font-weight: 900;
    color: rgb(112,112,112);
    text-decoration: underline;
    text-underline-offset: 4px;
    text-decoration-thickness: 2px;
    text-decoration-color: rgb(112,112,112);
    letter-spacing: -2px;
    line-height: 1.15;
    display: inline-block;
    padding: 4px 0;
    transition: color 0.2s ease, text-decoration-color 0.2s ease;
  }
  .site-menu-links li a:hover {
    color: #F3F3F0;
    text-decoration-color: #F3F3F0;
  }

  .site-menu-bottom {
    position: absolute;
    bottom: 28px;
    display: flex;
    flex-direction: column;
    align-items: center;
    gap: 8px;
  }
  .site-menu-socials {
    display: flex;
    gap: 10px;
    align-items: center;
  }
  .site-menu-socials a {
    font-size: 11px;
    letter-spacing: 0.12em;
    text-transform: uppercase;
    color: rgba(255,255,255,0.4);
    text-decoration: none;
    font-family: "BSP Helvetica", sans-serif;
    transition: color 0.2s;
  }
  .site-menu-socials a:hover { color: rgba(255,255,255,0.7); }
  .site-menu-dot { color: rgba(255,255,255,0.2); font-size: 9px; }

  /* Tablet — la hamburguesa ya está centrada vertical (top:50% + translateY) */
  @media (max-width: 768px) {
    .site-header { height: 90px; padding: 0 24px; }
    /* En móvil el header se mantiene transparente y solo visible arriba del todo */
    .site-header.scrolled {
      height: 90px;
      background-color: transparent;
      backdrop-filter: none;
      -webkit-backdrop-filter: none;
    }
    .site-header.header-hidden { transform: translateY(-100%); }
    .site-logo img { height: 40px; }
    .site-header.scrolled .site-logo img { height: 26px; }
    .site-hamburger { right: 20px; }
    .site-menu-close { top: 45px; right: 20px; }
    .site-menu-links li a { line-height: 1.15; letter-spacing: -1px; }
    .site-menu-bottom { bottom: 20px; }
  }

  /* Mobile */
  @media (max-width: 480px) {
    .site-header { height: 80px; }
    .site-header.scrolled { height: 56px; }
    .site-logo img { height: 34px; }
    .site-header.scrolled .site-logo img { height: 24px; }
    .site-hamburger { right: 16px; }
    .site-menu-close { top: 40px; right: 16px; }
  }
</style>
